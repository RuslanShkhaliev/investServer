export enum AssetType {
    DEFAULT = 'default',
    STOCK = 'stock',
    BOND = 'bond',
    CRYPTO = 'crypto',
    CURRENCY = 'currency',
}

export enum CallableStatus {
    IS_CALLABLE = 'callable',
    NON_CALLABLE = 'non-callable'
}
